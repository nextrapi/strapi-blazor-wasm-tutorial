<button class="btn btn-dark btn-lg btn-block" disabled="@Disabled" type="submit">@Title</button>

@code {
    [Parameter]
    public string Title { get; set; } = "";
    [Parameter]
    public bool Disabled { get; set; } = false;

}